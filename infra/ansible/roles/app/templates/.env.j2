# Generated by Ansible. Do not edit on server.

APP_NAME="{{ app_name | default('Rumah Integritas') }}"
APP_ENV=production
APP_KEY={{ app_key | default('') }}
APP_DEBUG=false
APP_URL={{ app_url | default('http://localhost') }}

APP_LOCALE=en
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=en_US

LOG_CHANNEL=stack
LOG_LEVEL=info

DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE={{ db_name | default('laravel') }}
DB_USERNAME={{ db_user | default('laravel') }}
DB_PASSWORD={{ db_password | default('secret') }}

SESSION_DRIVER=file
QUEUE_CONNECTION=database
CACHE_STORE=file

# WebSockets / Reverb (optional)
BROADCAST_CONNECTION={{ broadcast_connection | default('log') }}
REVERB_APP_ID={{ reverb_app_id | default('app') }}
REVERB_APP_KEY={{ reverb_app_key | default('changeme') }}
REVERB_APP_SECRET={{ reverb_app_secret | default('changeme') }}
REVERB_HOST=0.0.0.0
REVERB_PORT=8080
REVERB_SCHEME=http

# Vite
VITE_APP_NAME="{{ app_name | default('Rumah Integritas') }}"
VITE_REVERB_APP_KEY={{ reverb_app_key | default('') }}
VITE_REVERB_HOST={{ server_name | default('localhost') }}
VITE_REVERB_PORT={{ reverb_port | default(8080) }}
VITE_REVERB_SCHEME={{ 'http' }}
VITE_REVERB_PATH=/app
